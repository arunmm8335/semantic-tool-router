{
  "tools": [
    {
      "name": "github_search_prs",
      "description": "Search pull requests by repository, state, author, review status, or CI terms.",
      "input_schema": {
        "repository": "owner/name",
        "query": "search text",
        "state": "open, closed, or all"
      },
      "examples": [
        "find open pull requests with failing CI",
        "search PRs waiting for review"
      ],
      "tags": ["github", "pull-requests", "code-review"],
      "permissions": ["network"],
      "cost": "remote"
    },
    {
      "name": "github_fetch_workflow_logs",
      "description": "Fetch GitHub Actions workflow jobs, steps, and logs for a failing commit or pull request.",
      "input_schema": {
        "repository": "owner/name",
        "run_id": "workflow run id"
      },
      "examples": [
        "debug a failed GitHub Actions check",
        "show logs for a failed CI job"
      ],
      "tags": ["github", "ci", "logs"],
      "permissions": ["network"],
      "cost": "remote"
    },
    {
      "name": "filesystem_read",
      "description": "Read local project files, list directories, and inspect source code.",
      "input_schema": {
        "path": "local file or directory path"
      },
      "examples": [
        "inspect the README file",
        "list source files in this project"
      ],
      "tags": ["filesystem", "local", "code"],
      "permissions": ["read"],
      "cost": "local"
    },
    {
      "name": "python_unittest",
      "description": "Run Python unit tests with the standard unittest runner.",
      "input_schema": {
        "test_path": "test module or directory"
      },
      "examples": [
        "run the test suite",
        "verify Python unit tests"
      ],
      "tags": ["python", "tests", "local"],
      "permissions": ["execute"],
      "cost": "local"
    },
    {
      "name": "web_search",
      "description": "Search the public web for current facts, documentation, news, prices, or citations.",
      "input_schema": {
        "query": "web search query"
      },
      "examples": [
        "look up the latest model documentation",
        "find current package release notes"
      ],
      "tags": ["web", "search", "current-information"],
      "permissions": ["network"],
      "cost": "remote"
    },
    {
      "name": "image_generate",
      "description": "Generate or edit bitmap images from a visual prompt or reference image.",
      "input_schema": {
        "prompt": "image generation instructions"
      },
      "examples": [
        "create a product mockup image",
        "generate an app hero background"
      ],
      "tags": ["image", "generation", "creative"],
      "permissions": ["network"],
      "cost": "remote"
    },
    {
      "name": "git_commit_history",
      "description": "Inspect local Git commit history, diffs, branches, authors, and changed files.",
      "input_schema": {
        "repository_path": "local repository path",
        "revision": "commit, branch, or revision range"
      },
      "examples": [
        "show the last commit",
        "compare changes between two branches"
      ],
      "tags": ["git", "history", "code"],
      "permissions": ["read"],
      "cost": "local"
    },
    {
      "name": "shell_execute",
      "description": "Execute a local shell command or project build process.",
      "input_schema": {
        "command": "shell command",
        "working_directory": "local directory"
      },
      "examples": [
        "run the application build",
        "execute a command in the terminal"
      ],
      "tags": ["shell", "terminal", "local"],
      "permissions": ["execute"],
      "cost": "local"
    },
    {
      "name": "database_query",
      "description": "Run a read-only SQL query against a relational database and return rows.",
      "input_schema": {
        "connection": "database connection name",
        "sql": "read-only SQL statement"
      },
      "examples": [
        "query customer records from PostgreSQL",
        "count orders grouped by status"
      ],
      "tags": ["database", "sql", "data"],
      "permissions": ["network", "read"],
      "cost": "remote"
    },
    {
      "name": "calendar_schedule",
      "description": "Create, update, or inspect calendar events and meeting availability.",
      "input_schema": {
        "title": "event title",
        "start": "event start time",
        "attendees": "participant email addresses"
      },
      "examples": [
        "schedule a meeting tomorrow",
        "find a free time on the team calendar"
      ],
      "tags": ["calendar", "meetings", "productivity"],
      "permissions": ["network", "write"],
      "cost": "remote"
    },
    {
      "name": "email_send",
      "description": "Draft and send an email message to one or more recipients.",
      "input_schema": {
        "recipients": "email addresses",
        "subject": "message subject",
        "body": "message body"
      },
      "examples": [
        "email the weekly project update",
        "send a message to my mentor"
      ],
      "tags": ["email", "communication", "productivity"],
      "permissions": ["network", "write"],
      "cost": "remote"
    },
    {
      "name": "pdf_extract_text",
      "description": "Extract readable text, tables, and document metadata from PDF files.",
      "input_schema": {
        "path": "local PDF file path",
        "pages": "optional page selection"
      },
      "examples": [
        "read the tables in this PDF report",
        "extract text from a research paper"
      ],
      "tags": ["pdf", "documents", "extraction"],
      "permissions": ["read"],
      "cost": "local"
    }
  ]
}
